{
  "name": "ai-bridge",
  "version": "0.11.2",
  "description": "MCP bridge exposing GPT (codex CLI) and Gemini (agy CLI) as review/exec/digest tools for cross-vendor dev workflows",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "smoke": "node scripts/smoke.mjs",
    "smoke:live": "node scripts/smoke.mjs --live"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  }
}
